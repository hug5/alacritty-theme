# // 2022-08-14 Sun 19:13
# // 2022-11-05 Sat 23:18
# Moved this over to its own file;
# The shell script will sed replace the import string below;
# The syntax should be something like this:
# - ~/.config/alacritty/themes/rust-shel


import:
    - ~/.config/alacritty/themes/mustard-drip
